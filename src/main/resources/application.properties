spring.application.name=sparkctl
spring.shell.interactive.enabled=true

# Spark / Hive JDBC defaults (overridable via CLI flags)
spark.host=localhost
spark.port=9030
spark.database=iceberg
spark.username=
spark.password=
